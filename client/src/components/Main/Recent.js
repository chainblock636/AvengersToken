import React from 'react'

const Recent = () => {
    const mystyle = {
        paddingBottom: "0px",
        paddingTop: "300px"
    };

    return (
        <section class="recent-winners" style={mystyle}>
        </section>
    )
}

export default Recent
